class Laberinto implements IVisualizable {
  /* --- ATRIBUTOS --- */
  private int xMaze = 10, yMaze = 50;
  private int bs = 45;
  private int line = 3;

  /* --- CONSTRUCTORES --- */
  public Laberinto() {
  }

  /* --- MÉTODOS --- */
  // Método para mostrar el laberinto
  void display() {
    //fill(94, 160, 84);
 /*   noStroke();
    //Rect x lb, y lb, ancho, largo
    fill(94, 160, 84);//vertical
    rect(xMaze, yMaze, line, 540);//1
    rect(xMaze + bs, yMaze, line, bs*2);//2
    rect(xMaze + bs*4, yMaze + bs, line, bs*4);//3
    rect(xMaze + bs*5, yMaze + bs, line, bs);//4
    rect(xMaze + bs*6, yMaze, line, bs*3);//5
    
    fill(84, 160, 145);//horizontal
    rect(xMaze + bs, yMaze, 530, line);//1
    rect(xMaze + bs, yMaze + bs*3, bs*3, line);
    rect(xMaze + bs*2, yMaze + bs, bs*3, line);
    rect(xMaze + bs, yMaze + bs*2, bs*2, line);
    rect(xMaze, yMaze + bs*4, bs*4, line);
  }*/

  /* --- METODOS ACCESORES --- */
}
